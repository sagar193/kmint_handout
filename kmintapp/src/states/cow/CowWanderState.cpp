#include "../../includes/states/WanderState.hpp"


WanderState::WanderState(cow &cow) : _cow(cow) {
	
}

void WanderState::act(kmint::delta_time dt) {
	//_cow.SetState(cow::STATE_NAMES::ATTACK_STATE);
}